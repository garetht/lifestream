<%= render "layouts/leftnavbar" %>

<section class="rightbar">

  <a href="<%= new_stream_post_url(@streamid) %>"><span class="new">new</span> <span class="postdot">post.</span></a>

<% @posts.each do |post| %>
  <%= render partial: "apost", locals: {post: post, photos: post.post_photos} %>
<% end %>

</section>